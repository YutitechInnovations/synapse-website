/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // If you have images, you might need to uncomment this:
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig; 